# 에이전트 일처리 속도가 느린 이유

## 구조적 원인

### 1. 헤드비트 모델 — 단속적 실행

에이전트는 연속 실행이 아니라 주기적으로 깨어나는 방식이다.

```
잠 → 깨어남 → 작업 → 잠 → 깨어남 → 작업 ...
```

`intervalSec: 300`이면 5분마다 한 번 깨어난다. 작업 중간에 잠들면 다음 실행까지 기다려야 한다.

---

### 2. 매 실행마다 컨텍스트 재구성

깨어날 때마다 실제 작업 전 오버헤드가 매번 발생한다:

- system prompt 로드 (SOUL.md + AGENTS.md + HEARTBEAT.md)
- `GET /api/agents/me` — 자신 확인
- `GET /api/issues?status=in_progress` — 작업 목록 조회
- 이전 상태 파악

---

### 3. 위임 체인의 레이턴시

```
사람 → CEO (깨어날 때까지 대기)
     → CEO가 CTO에게 위임 (CTO 깨어날 때까지 대기)
     → CTO가 Engineer에게 위임 (Engineer 깨어날 때까지 대기)
     → Engineer 실행
```

각 단계마다 heartbeat 주기만큼 지연이 쌓인다.

---

### 4. 단일 assignee + checkout 모델

한 태스크에 한 에이전트만 붙을 수 있고, checkout으로 잠긴다. 병렬 처리가 제한된다.

---

### 5. LLM 응답 시간

Claude CLI 실행 자체가 수십 초~수 분 소요된다. 복잡한 작업일수록 더 길다.

---

### 6. 승인 게이트

에이전트 고용, 예산 초과 등은 사람 승인을 기다려야 한다.

---

## 개선 가능한 부분

| 원인 | 개선 방법 |
|------|----------|
| 긴 heartbeat 주기 | `intervalSec` 줄이기 (예: 60~120) |
| 위임 단계 과다 | CEO가 직접 Engineer에게 위임하도록 구조 단순화 |
| 직렬 위임 | 독립적인 작업은 여러 에이전트에 동시 할당 |
| 컨텍스트 오버헤드 | `promptTemplate` 간결하게 유지 |
